import React from "react";
import UseAxiosPrivate from "../UseAxiosPrivate";
import { useQuery } from "@tanstack/react-query";

const GetSingleAirline = () => {
  const { axiosPrivateUrl } = UseAxiosPrivate();
};

export default GetSingleAirline;
